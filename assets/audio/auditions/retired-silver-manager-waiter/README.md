# Retired Silver Room manager takes

These ten takes used the shared `waiter` profile and were removed from the
runtime index after the manager voice failed playtest. They are retained here
only as recoverable reference. The active manifest assigns the manager bank to
the distinct `npc-male` audition profile; approved replacements should be
delivered under the original `vo.silver.manager.*.mp3` filenames in
`assets/sfx/`.
