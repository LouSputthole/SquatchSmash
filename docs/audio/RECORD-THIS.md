# RECORD THIS

**0 files to generate: 0 spoken lines and 0 effects.** 0 of the spoken lines are RE-RECORDS — a take already exists on disk and says the wrong words.

Generated by `npm run record:sheet`. Do not hand-edit — fix the authored line and regenerate.

`docs/audio/RECORD-THIS.csv` is the same list, one row per file, with the voice id and
settings on every row. That is the one to feed a generator.

## Deliver it exactly like this

Every file goes in `assets/sfx/` **under the exact filename shown**. Then:

```
npm run sfx:listen      # rebuild assets/sfx/index.json and audition
npm run voice:needed    # what is left
npm run record:sheet    # regenerate this file
npm test && npm run check
```

A re-record overwrites the file that is already there. Once its replacement is indexed,
delete that line's entry from `assets/sfx/rerecord.json` and run `npm run vo:rerecord`,
or `npm run check` will keep reporting the take as stale.

## Do NOT record the legacy queue

`VOICE-LINES-TODO.md` lists 83 sound effects and 13 ambience/music briefs under a legacy
review queue. They are not in this file on purpose. The effects are synthesised live in
WebAudio and have no sample-lookup path, and the ambience and music briefs have no code
hook at all — generating them today produces files nothing can play. Promoting them is a
playback-code project, not a recording run.

