node_modules
_*.mjs
# package-lock.json is COMMITTED (un-ignored 2026-08-14): the Verify workflow
# runs `npm ci`, which requires it, and a pinned tree is the point of CI.

# Local provider credentials. Keep an example file explicitly opt-in if one is
# ever added; generated-audio keys must never enter source control.
.env
.env.*
!.env.example

# Single-file builds from `npm run bundle`. A few megabytes of base64, and
# rebuildable in one command, so they do not belong in the history.
dist/

# Browser verifier screenshots are local release evidence, not runtime files.
artifacts/

# Generated sound effects are large and rebuildable from assets/sfx/manifest.json
# with `npm run sfx`. Uncomment to keep them out of the repo:
# assets/sfx/*.mp3

# Regenerate with: npm run sfx:listen
assets/sfx/_listen.html
.claude/worktrees/

# Scratch probes. Agents and humans both end up writing a throwaway script that
# builds one scene and measures it; those are working notes, not deliverables,
# and they should never reach a commit.
.tmp/

# Verifier evidence output. Every browser gate rewrites its captures --
# PNGs and report.json alike -- on every run, so a full sweep dirties the
# tree just by looking at it. New run output stays out of the index; the
# evidence already committed stays tracked (gitignore never hides tracked
# files) because the evidence-contract tests read it.
docs/validation/
