# Campground game assets

Drop a transparent PNG here as `logo.png` and it appears on the menu screen.
Nothing else is required — the game generates everything else at runtime.

(The apartment's assets live in the repo root's `assets/` folder instead; the
two are kept separate so the game stays self-contained.)
